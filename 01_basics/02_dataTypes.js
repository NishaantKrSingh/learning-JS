"use strict"; // treat all JS code as newer version

// alert("") // can't be used because we're using node js and not in browser

console.log(3
    +
    3) // code readibility should be high so this is not a good example

let name = "Nishant"
let age = 18
let isLoggedIn = false
let state;

// number => 2 to the power 53
// bigint => generally used to deal with very large numbers
// string => is surrounded with ""
// boolean => true/false
// null => standalone value
// undefined => is something that is not defined
// symbol => unique

console.log(typeof undefined); //undefined
console.log(typeof null); //object
// is considered as a flaw of the language
